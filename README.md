# VimConfig

## vim

Go to _/vim/_ execute install-xxx.sh depends on you operating system.

## nvim

See _/nvim/README.md_ for full setup and keymaps.

### Sync to system

Run in _/nvim/_:

```bash
./sync-to-system.sh
```

The sync is direct overwrite to `~/.config/nvim` (no backup is created).
